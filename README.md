# Tone Generator

I needed a super simple tone generator. This is it.

FYI, it deploys to https://eschiebel.github.io/tone-generator using github actions defined in .github/workflows/pages.yml
so all you need to do is `npm run build` and push to main.
